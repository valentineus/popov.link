{
  "name": "@valentineus/website",
  "private": "true",
  "scripts": {
    "lint": "cross-env-shell stylelint \"sass/**/*.scss\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@git.popov.link/valentineus/website.git"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "stylelint": "^14.9.1",
    "stylelint-config-standard-scss": "^5.0.0"
  }
}